import React, { Component } from "react";
import data from "./data";

export default class Edit extends Component {
  constructor(props) {
    super(props);
    this.state = {
      dataApi: data,
    };
  }
  render() {
    return <div>{}</div>;
  }
}
